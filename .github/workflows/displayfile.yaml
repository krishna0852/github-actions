name: display file 

on:
 workflow_dispatch:

jobs:
    dispfile:
        runs-on: ubuntu-latest
        steps:
            - name: checkout the code
              uses: actions/checkout@v2
            - name: display file
              run: cat testfile
            #   working_directory: ./